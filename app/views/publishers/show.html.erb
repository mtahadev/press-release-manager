<p class="text-success"><%= notice %></p>

<% if @publisher.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <ul>
      <% @publisher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= render @publisher %>

<div class="mt-5">
  <div class="row">
    <div class="col-2">
      <%= link_to "Edit this publisher", edit_publisher_path(@publisher), class: "btn btn-primary mr-2" %>
    </div>
    <div class="col">
      <%= button_to "Destroy this publisher", @publisher, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
    </div>
  </div>
</div>
